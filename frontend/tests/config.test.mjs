import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";test("reader sends its JWT when saving progress",()=>{const s=fs.readFileSync("app/reader/[id]/reader.tsx","utf8");assert.match(s,/panelium_token/);assert.match(s,/Bearer/);assert.match(s,/type="range"/)});test("admin route supports editorial uploads",()=>{const s=fs.readFileSync("app/admin/page.tsx","utf8");assert.match(s,/admin\/works/);assert.match(s,/multiple/)});test("metadata identifies Panelium",()=>{assert.match(fs.readFileSync("app/layout.tsx","utf8"),/Panelium/)});

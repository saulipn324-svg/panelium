import {NextRequest,NextResponse} from "next/server";export async function PUT(req:NextRequest){const body=await req.json();const token=req.headers.get("authorization");if(!token)return NextResponse.json({message:"Inicia sesión para guardar tu progreso"},{status:401});const base=process.env.PANELIUM_API_URL??"http://localhost:8080";const r=await fetch(`${base}/api/progress/${body.workId}`,{method:"PUT",headers:{"Content-Type":"application/json","Authorization":token},body:JSON.stringify({chapterId:body.chapterId,pageNumber:body.pageNumber})});return NextResponse.json(await r.json(),{status:r.status});}

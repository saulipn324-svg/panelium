spring:
  datasource:
    url: jdbc:h2:mem:panelium;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway.enabled: false
  jpa:
    hibernate.ddl-auto: create-drop
    open-in-view: false
